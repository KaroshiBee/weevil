(library
 (name mdb_expect_tests)
 (libraries
   adapter
   dapper
   defaults
   mdb
   test_utils
   lwt
   lwt.unix
   conduit-lwt-unix
   data-encoding
   cmdliner
   logs
   logs.lwt
   ppx_expect
   str
 )
 (inline_tests (deps (glob_files data/*.tz)))
 (preprocess (pps lwt_ppx ppx_expect))
)
